import { Meta, Story } from '@storybook/addon-docs';

import { Gantt } from '../src';

import { CustomIcons } from './CustomIcons';

<Meta
  title="CustomIcons"
  component={Gantt}
/>

export const Template = (props) => {
  return <CustomIcons {...props} />;
}

# Custom шcons example

<Story name="CustomIcons">
  {Template.bind({})}
</Story>
