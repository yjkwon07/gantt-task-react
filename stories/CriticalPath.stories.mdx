import { Meta, Story } from '@storybook/addon-docs';

import { Gantt } from '../src';

import { CriticalPath } from './CriticalPath';

<Meta
  title="CriticalPath"
  component={Gantt}
/>

export const Template = (props) => {
  return <CriticalPath {...props} />;
}

# Critical path example

<Story name="CriticalPath">
  {Template.bind({})}
</Story>
